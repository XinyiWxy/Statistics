
import numpy as np 

def phi(k):
    
    d1 = np.random.uniform(0, 1, k)
    d2 = np.random.uniform(0, 1, k)

    c_count = np.sum(d1 ** 2 + d2 ** 2 <= 1)
    
    total_count = k

    phi = 4 * c_count/total_count
    return phi

N = 1000000
print(phi(N))
